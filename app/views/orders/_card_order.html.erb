<div class="card-order">
  <%= image_tag ""%>
  <div class="card-order-info"></div>
    <div class="card-product-order-infos">
      <h2><%= order.flower.name %></h2>
      <p>CBD: <%= order.flower.cbd_ratio.round(1) %>%</p>
      <p><%= order.flower.price %>€</p>
    </div>
    <div class="destroy-info">
      <%= link_to order_path(order.id), method: :delete, data: { confirm: "Sure?" }, class: 'pull-right btn btn-default' do %>
        <i class="fas fa-trash-alt"></i>
      <% end %>
    </div>
</div>
