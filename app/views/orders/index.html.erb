<h1>My orders</h1>

<ul>
  <% @orders.where(paid: true).each do |order| %>
    <li><%= render "../users/card_order", order: order %></li>
  <% end %>
</ul>

<% if false %>
  <ul>
    <li><h2>Order n° : <%= @order.id %></h2></li>
    <li>Order amount : <%= order.total_amount %></li>
    <li>Order date : <%= order.created_at %></li>
    <li>Paid ? <%= order.paid %></li>
  </ul>
<% end %>

<%= link_to "Back", :back %>
